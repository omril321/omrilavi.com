{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    // Override only the problematic setting for Cypress compatibility
    "moduleResolution": "node",
    // Add Cypress-specific types
    "types": ["cypress", "node"]
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ]
}
