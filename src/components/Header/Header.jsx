import * as React from 'react';
import AboutMe from "../Homepage/AboutMe";

const Header = () => (
    <header className="px-8 py-4 max-w-4xl sm:py-10 lg:pb-16">
        <AboutMe/>
    </header>
);

export default Header;